# [MS-RDPBCGR] 4.2.2 Server Shutdown Request Denied PDU
# Annotated example between an RDP 5.1 client and server (Standard RDP Security).
# SPEC ERRATUM: the dump's TPKT length is 0x0024 (36), and the annotation agrees, but the
# dump holds 44 bytes and MCS userData length 30 implies 44. The wire file keeps the bytes
# as printed and fails the TPKT length check on purpose. The -decrypted rebuild
# uses the real size 44 as its 'before' value.
# Spec annotation also says flags 0x0008; the dump bytes are 08 08 (0x0808); bytes kept.
# TLS-mode rebuild of the RC4 example (the spec's decrypted dump plus new framing):
#   - removed the 12-byte non-FIPS security header (flags, flagsHi, 8-byte MAC)
#   - MCS userData length and TPKT length recomputed (TPKT 44 -> 32)
#   - every other byte is unchanged from the spec's decrypted dump
#    0  TPKT length 32; 4 X.224 DT; 7 MCS SendDataIndication (0x68): initiator 1002, channel 1003, prio high, begin|end, length 18
#   14  share control: totalLength 18, pduType 0x0017 (DATAPDU | version), pduSource 1002
#   20  share data: shareID 0x000203ea, pad1 0xa6, streamId 2, uncompressedLength 18,
#       pduType2 37, compressedType 0, compressedLength 0
#   32  (no body: the PDU is only the share data header)

S> 03 00 00 20 02 f0 80 68 00 01 03 eb 70 12 12 00
   17 00 ea 03 ea 03 02 00 a6 02 12 00 25 00 00 00
